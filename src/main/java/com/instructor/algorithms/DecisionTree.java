package com.instructor.algorithms;

// TODO: COMPLETE DECISION TREE FOR CLASSIFYING CORRECT/INCORRECT POSE
public class DecisionTree {

    private class Node {
        Node() {

        }
    }

    private Node root;

    /**
     * Construct a decision tree based on features like angle, speed, and keypoints
     */
    public DecisionTree() {
        root = new Node();
    }
}
